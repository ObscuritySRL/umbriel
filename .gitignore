# dependencies
node_modules

# scratch / working dirs — never commit
/.scratch/
.claude/
AGENTS.*.md
ignore*

# build output
out
dist
*.tgz
*.tsbuildinfo

# logs / coverage
logs
*.log
coverage
*.lcov

# dotenv
.env
.env.*.local
.env.local

# caches
.cache

# editors / OS
.idea
.DS_Store
*.stackdump

# Local-only dev docs — never publish
ARTICLE.md
DISCORD_POST.md

# Local-only dev scripts — never publish (kept on disk, out of git + the published tarball)
scripts/chatgpt-imagegen.ts
