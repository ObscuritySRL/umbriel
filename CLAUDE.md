# CLAUDE.md

Claude Code reads this file automatically; it does not read `AGENTS.md`. The project's
rules live in `AGENTS.md` (the single source of truth, shared with every other agent/tool).
The line below imports that file's full contents into context — read and follow them exactly.

@AGENTS.md
