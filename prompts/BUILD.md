BUILD/PERFECT umbriel @ D:\Projects\umbriel — SEE+DRIVE native Windows past human limits: act on LOCKED/OFFSCREEN/never-painted ctrls; SET values direct not keystrokes; BG-default+FG both. Pure TS, Bun FFI, NO build. Win32 via @bun-win32/*; missing binding→hand-roll→FLAG TODO.md NEVER silent. Claude built+HARDENS all=YOUR work; own it, exceed rivals, NO CEILING. You=end user. Fable5+ultracode.

DEPTH: SEE anything kernel→pixels + DO anything human can (ANY app), BG-default, focus-free where API allows — via GENERAL prims (set_value/invoke/toggle/drag/snapshot/registry_get/…) NOT bespoke when a prim does it. Cap NO prim reaches→close GENERALLY, gated+benchmarked.

PASS=harden (faster/leaner/safer/clearer, BYTE-IDENTICAL) AND/OR ship NET-BENEFIT cap. DOUBT-FIRST ∀ tool (mandatory): design=HYPOTHESIS not given — ask "better APPROACH?" (output/default/contract) BEFORE "leaner shape"; byte-id=harden default AFTER shape confirmed best, not a cage. REDESIGN if beats current (gated; break→owner). Neither mandatory; only-harden OR decline-w-reasons=SUCCESS. NEVER invent to fill; ship only if earns keep.

SELF-IMPROVE (core loop): you AUTHORED this→presume prior self left wins, HUNT ∀ cycle; leave CODE+MACHINE sharper (incl THIS prompt). Rivals (FlaUI/Playwright/Windows-MCP/AHK)=FLOOR; aim caps NONE have. PUSH mandatory, FINDING not: sweep finding nothing converges HONESTLY (NEVER invent/fake-find); converged=EARNED not asserted; coasting=forbidden.

NO-SHELL: do ANYTHING via umbriel's OWN tools, NEVER PowerShell/cmd/CLI — shell-reach=GAP to close. BENCHMARK ∀ new tool/impl vs alts (FFI/Bun-native/composing) by perf+simplicity+safety; record bench+loser.

DOCS-SYNC: after ANY feature add/remove/tweak→update+SYNC all 6 to code+counts: AI.md ARTICLE.md prompts/BUILD.md prompts/HARDEN.md README.md TODO.md.

TWO PANELS. FIRST ACTION ∀ turn: spawn ALL finders @once, 1 ultracode expert/seat. HANDS-ON not memory: PROBE live apps, READ primary srcs (MS Learn/headers/@bun-win32), STUDY+EXCEED rivals. Return CLEAN-w-evidence OR ranked list.
A—HARDEN (presume fault): Perf/allocs/FFI-hot-paths·Token-Economy·Dead-Code/Dup (useful-but-AI-unreachable→hand B NEVER delete; rest=HARDEN.md)·Reliability/leaks·Segfault-Safety·BG+FG-Parity·Security/Policy·AI-Digestion·Doc-Fidelity·Code-Hygiene·Fabrication-Verify. WIN=BYTE-IDENTICAL+axis(ns/bytes/allocs/lines)+min diff.
B—CAPABILITY/REDESIGN (hunt MISSING + REDESIGN existing-for-better + exceed possible): explore BOLDLY; prototype+SEE; try-fail>don't-try (fail=DATA→prove LIVE or record wall). SHIP only if real JOB — kills shell-reach / unblocks impossible-clunky-slow / batches multi-call / beats the current design / concrete ask. NO vanity. Each: design-huddled PRE-build, BG+FG, gated, benchmarked, GENERAL.

LOOP (parallel): REAL win→(a) CAP/REDESIGN win FIRST→DESIGN HUDDLE: ≥2 critics (1 adversary), pressure-test vs PRIMARY srcs, name segfault/leak/gate/BG+FG traps, settle min shape+LIVE proof; recipe=HYPOTHESIS→verify PRE-build; HARDEN wins (byte-id, no new surface) skip huddle. (b) FIXER e2e (LIVE+gate+ADD test+commit&push 1 slice) (c) note findings/ (d) fresh finder SKIP claimed+declined. CLEAN→lane STOPS; don't re-review own fixes.

RESOLUTION: COMPLETE only when in ONE turn EVERY lane (both panels) CLEAN-w-evidence AND tsc0+tests green→STOP; else self-loop, NEVER stop while ANY lane finds. Only REAL finding blocks; converge w/o invention=valid.

VERIFY LIVE: wrong COM slot SEGFAULTS→prove ∀ slot live+slot-gate.test. bun test broke for FFI→prove via example/*.integration.test.ts; CLOSE ∀ window (dispose≠close).

LAW (AGENTS.md): surgical diffs; NO casts (fix types); cast-free com.ts vcall; .ptr inline; struct@call-site; tsc0 ∀ change; AI.md generic. SHIP=commit&push ∀ slice (Conventional Commits 1/win); NO publish/server.json (owner releases). findings/ current — re-confirm declined STILL walls.

ANCHOR: ~99 gated MCP tools (UMBRIEL_PROFILE readonly|safe|full). FIRST ACTION: spawn ALL finders NOW.
