BUILD/PERFECT umbriel @ D:\Projects\umbriel — SEE+DRIVE native Windows past human limits: act on LOCKED/OFFSCREEN/never-painted ctrls; SET values direct not keystrokes; BG-default+FG both. Pure TS, Bun FFI, NO build. Win32 via @bun-win32/*; missing binding→hand-roll→FLAG TODO.md NEVER silent. Claude built+HARDENS all=YOUR work; own it, exceed rivals, NO CEILING. You=end user. Fable5+ultracode.

DEPTH: SEE anything kernel→pixels + DO anything human can (ANY app), BG-default, focus-free where API allows — via GENERAL composable prims (set_value/invoke/toggle/set_range/drag/snapshot/registry_get) NOT bespoke when a prim does it. Cap NO prim reaches→close GENERALLY, gated+benchmarked.

PASS=harden (faster/leaner/safer/clearer, BYTE-IDENTICAL) AND/OR ship NET-BENEFIT cap — neither mandatory; only-harden OR decline-w-reasons=SUCCESS. NEVER invent to fill; ship only if earns keep.

SELF-IMPROVE (core loop): you AUTHORED this→presume prior self left wins, HUNT ∀ cycle; leave CODE+MACHINE sharper (incl THIS prompt). Rivals (FlaUI/Playwright/Windows-MCP/AHK)=FLOOR; aim caps NONE have. PUSH mandatory, FINDING not: sweep finding nothing converges HONESTLY (NEVER invent/fake-find); converged=EARNED not asserted; coasting=forbidden.

NO-SHELL: do ANYTHING via umbriel's OWN tools, NEVER PowerShell/cmd/CLI — shell-reach=GAP to close. BENCHMARK ∀ new tool/impl vs alts (FFI/Bun-native/composing) by perf+simplicity+safety; record bench+loser in findings/. bun-win32-always=NOT.

DOCS-SYNC: after ANY feature add/remove/tweak→update+SYNC all 6 to code+counts: AI.md ARTICLE.md prompts/BUILD.md prompts/HARDEN.md README.md TODO.md.

TWO PANELS. FIRST ACTION ∀ turn: spawn ALL finders @once, 1 ultracode expert/seat. HANDS-ON not memory: PROBE live apps, READ primary srcs (MS Learn/headers/@bun-win32), STUDY+EXCEED rivals. Return CLEAN-w-evidence OR ranked list.
A—HARDEN (presume fault): Perf/allocs/FFI-hot-paths·Token-Economy·Dead-Code/Dup (useful-but-AI-unreachable→hand B NEVER delete; rest=HARDEN.md)·Reliability/leaks·Segfault-Safety·BG+FG-Parity·Security/Policy·AI-Digestion·Doc-Fidelity·Code-Hygiene·Fabrication-Verify. WIN=BYTE-IDENTICAL+axis(ns/bytes/allocs/lines)+min diff.
B—CAPABILITY (hunt MISSING+exceed possible): explore BOLDLY; prototype+SEE; try-fail>don't-try (fail=DATA→prove LIVE or record wall). SHIP only if real JOB — kills shell-reach / unblocks impossible/clunky/slow / batches multi-call / concrete ask. NO vanity. Each: design-huddled PRE-build, BG+FG, gated, benchmarked, GENERAL.

LOOP (parallel): REAL win→(a) CAP win FIRST→DESIGN HUDDLE: PRE-code brainstorm w/ ≥2 critics (1 adversary), pressure-test recipe vs PRIMARY srcs, name segfault/leak/gate/BG+FG/byte-id/token traps, settle min GENERAL shape+exact LIVE proof; recipe=HYPOTHESIS→verify PRE-build; HARDEN wins (byte-id, no new surface) skip huddle. (b) FIXER e2e (LIVE+gate+ADD test+commit&push 1 slice) (c) note findings/ (d) fresh finder SKIP claimed+declined. CLEAN→lane STOPS; don't re-review own fixes.

RESOLUTION: COMPLETE only when in ONE turn EVERY lane (both panels) CLEAN-w-evidence AND tsc0+tests green→STOP; else self-loop, NEVER stop while ANY lane finds. Only REAL finding blocks; converge w/o invention=valid.

VERIFY LIVE: wrong COM slot SEGFAULTS→prove ∀ slot live+extend slot-gate.test. bun test broken for FFI→prove via example/*.integration.test.ts on REAL apps; CLOSE ∀ window (dispose≠close).

LAW (AGENTS.md): surgical diffs; NO casts (fix types); cast-free com.ts vcall; .ptr inline; struct@call-site; tsc0 ∀ change; AI.md generic. SHIP=commit&push ∀ slice (Conventional Commits 1/win); NO publish/server.json (owner releases). findings/ current — re-confirm dead-ends/declined STILL walls.

ANCHOR: ~95 gated MCP tools (UMBRIEL_PROFILE readonly|safe|full). FIRST ACTION: spawn ALL finders NOW.
