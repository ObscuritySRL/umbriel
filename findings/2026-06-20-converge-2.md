# converge-2 — re-audit tonight's changes + continue lanes + verify the vision live — 2026-06-20

A 5-seat fresh fleet after the night's first ships. Result: 3 lanes CLEAN-with-evidence, the select parity fix SHIPPED,
2 doc-fidelity fixes SHIPPED, 2 owner/follow-on constraints recorded. The "total OS control via general primitives"
vision was VERIFIED LIVE — no bespoke tool is warranted.

## CLEAN-with-evidence (re-confirmed lanes)
- **Regression (tonight's 2 code changes):** CLEAN. registry_set `data` masking is type-aware + surgical (only the
  REG_SZ/EXPAND_SZ string payload collapses; DWORD/QWORD integer stays legible; MULTI_SZ already collapsed; `data` is a
  registry_set-only arg; tool RESULT byte-identical). launch() ShellExecuteW fallback is additive ($PATH path unchanged,
  catch only on spawn-throw, no leak, can't mis-launch). tsc 0, 48 unit pass, both new tests pass.
- **Perf/Redundancy/Token/Segfault (steady):** CLEAN. slot-gate 144 (104+22+18) verified, 0 mismatched, two-way drift
  green; tsc 0; biome clean; no hot-loop alloc / no .ptr-across-await / no cast in either changed file; the prior
  REJECTED-below-bar perf items + REJECTED-UNSAFE BSTR toBuffer still stand (those files untouched).
- **Capability / vision (verify-total-os-control-via-general-primitives):** CLEAN — the vision is ALREADY REAL via the
  EXISTING general primitives; NO bespoke tool warranted (owner was explicit: never build set_volume/toggle_wifi/
  get_timezone). VERIFIED LIVE (non-destructive): VOLUME — opened Quick Settings (sendKeys Win+A), attached
  className 'ControlCenterWindow', found the "Sound output" Slider (RangeValuePattern, rangeValue=18/0..100), called
  setRangeValue(18) (same value → S_OK, read-back 18, zero net change, no audio blast), closed with Escape — the general
  set_range primitive REACHES + SETS system volume. OS observability — registry_get read TimeZoneKeyName="Eastern
  Standard Time"/ActiveTimeBias=240 and ActivePowerScheme GUID; system_status + system_resources returned locks/
  screensaver/monitors/RAM/CPU/uptime/process-count. Every vision item (RAM/CPU/locks/screensaver/displays/processes/
  services/tasks/env/logs/timezone/power) maps to a shipped GENERAL tool. No true gap.

## SHIPPED this round
- **select parity disclosure** (`da5c35f`) — SelectionItem.Select on a classic own-HWND item (radio/list-item/tab/cell)
  takes the same SetFocus foreground steal invoke/toggle/set_value disclose, but `select` reported a flat "cursor-free".
  Live-proven (3 runs + the shipped example/select-no-raise.integration.test.ts synthetic radio: select() moved fg to
  the radio's OWN HWND, parent stayed minimized, BM_GETCHECK==1). Fixed by an optional 4th `stealNote` param on
  disclosingPatternAct (default branch byte-identical → invoke/toggle/set_value unchanged, guarded by pattern-no-raise +
  mcp-pattern-no-raise still PASS) + routing the act()/handler select verbs through it with SELECT_STEAL_NOTE, plus
  doc corrections (tool desc, JSDoc, AI.md:43/88). Shipped under the owner's session-wide authority grant for
  byte-changing fixes.
- **2 doc-fidelity fixes** (`d65a0a8`) — AI.md:127 masked-args enumeration gained registry_set (tonight's f0051a8 synced
  the code comment but missed the AI.md mirror); HARDEN.md:27 ANCHOR's 21-commit-stale HEAD SHA replaced with a SHA-free
  invariant so it can't rot again (the 83-tool/tsc-0/48-test facts re-verified still true).

## Owner / follow-on CONSTRAINTS (recorded — re-confirm, do not re-litigate blindly)
- **Secondary parity (click cursor-free chain) — DEFERRED follow-on.** clickElement's cursor-free activation fallback
  (mcp.ts:1290-1291 toggle, 1296-1297 select) calls element.toggle()/element.select() RAW (not via toggleSmart/
  disclosingPatternAct), so on a classic own-HWND control it returns "toggled/selected (cursor-free)" with the SAME
  undisclosed steal. Pre-existing, broader than the select verb, medium-confidence. Now that the disclosingPatternAct
  4th-param + SELECT_STEAL_NOTE exist, the fix is small (route those 2 calls through disclosingPatternAct with the
  default note for toggle / SELECT_STEAL_NOTE for select) — but it needs its own clickElement-fallback live test, and the
  converge-2 seat recommended flag-only (don't widen scope without a dedicated test). Left for a focused fixer.
- **server.json omits UMBRIEL_TRACE_SNAPSHOTS — OWNER-RESERVED.** mcp.ts reads 10 UMBRIEL_* env vars; server.json
  documents 9 (TRACE_SNAPSHOTS, read at mcp.ts:298, missing). server.json is release-coupled (version lockstep +
  mcp-publisher republish), so it stays the owner's — not shipped autonomously per the goal's "NO server.json" rule.
  Fix described for the owner: append one environmentVariables[] entry after UMBRIEL_TRACE, then bump version + republish.

## Process lesson (recorded so it isn't repeated)
A converge-2 seat's `rm -f .scratch/select-*.ts` cleanup glob misfired and EMPTIED the gitignored .scratch/ dir,
deleting throwaway probes (discord-*, paint-*, slate-final) AND the keep-awake.ts source. The keep-awake PROCESS
(PID 59652) survived (a running Bun process doesn't need its source file) so the PC stayed protected, and keep-awake.ts
was recreated. LESSON: scratch cleanup must rm only explicitly-created files by EXACT name, NEVER a wildcard over a
shared scratch dir.
